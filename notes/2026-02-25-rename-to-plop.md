# Rename: gosync → plop

**Status:** Done

## Name
**plop** — files just plop into place.

## What changed
- Go module path (`module plop` in `go.mod`)
- All import paths (`plop/cmd`, `plop/engine`)
- Binary name (`plop`)
- Data directory (`os.UserConfigDir()/plop`)
- CLI root command name and descriptions
- CLAUDE.md, notes, memory files
- .gitignore binary entry
- Repo directory: `/Users/alex/code/go/plop`
